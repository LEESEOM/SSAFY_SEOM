# def make_perm2(idx,cards):
#     if idx == len(cards):
#         return check_rt(cards)
#     for i in range(idx, len(cards)):
#         cards[idx], cards[i] = cards[i], cards[idx]
#          make_perm2(idx+1,cards)
#         cards[idx], cards[i] = cards[i], cards[idx]



